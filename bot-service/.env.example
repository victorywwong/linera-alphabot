# Strategy Selection
# Options:
#   simple-ma        - Baseline SMA crossover (deterministic)
#   deepseek         - DeepSeek V3 via API (Atoma-supported)
#   qwen-vertex      - Qwen 3 Coder 480B via Vertex AI SDK (RECOMMENDED, Atoma-supported)
#   gpt-oss-vertex   - GPT OSS 120B via Vertex AI SDK (Atoma-supported)
STRATEGY=simple-ma

# Market Data APIs
# Using Binance (no API key required for public endpoints)
BINANCE_BASE_URL=https://api.binance.com

# LLM Strategies (Wave 2+)

# Schematron (Atoma inference network - RECOMMENDED for Wave 2+)
# Get API key from: https://inference.net
# Cost: ~$0.20/month for hourly predictions (cheapest option!)
# STRATEGY=schematron
SCHEMATRON_API_KEY=sk-...
SCHEMATRON_BASE_URL=https://api.inference.net
SCHEMATRON_MODEL=schematron-3b

# DeepSeek V3 (Alternative LLM provider)
# Get API key from: https://platform.deepseek.com
# Cost: ~$0.70/month for hourly predictions
# STRATEGY=deepseek
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Vertex AI Strategies (RECOMMENDED for Wave 2)
# Uses OpenAI SDK with Vertex AI's OpenAI-compatible MaaS endpoint
# See: https://docs.cloud.google.com/vertex-ai/generative-ai/docs/maas/call-open-model-apis
#
# Prerequisites:
# 1. Install gcloud CLI: https://cloud.google.com/sdk/docs/install
# 2. Authenticate: gcloud auth application-default login
# 3. Set project: gcloud config set project YOUR_PROJECT_ID
# 4. Enable Vertex AI API: gcloud services enable aiplatform.googleapis.com
#
# Endpoint patterns:
# Qwen (regional):  https://us-south1-aiplatform.googleapis.com/v1/projects/PROJECT_ID/locations/us-south1/endpoints/openapi
# GPT OSS (global): https://aiplatform.googleapis.com/v1/projects/PROJECT_ID/locations/global/endpoints/openapi
#
GCP_PROJECT_ID=your-gcp-project-id
#
# Option A: Qwen 3 Coder 480B (RECOMMENDED - Best for technical analysis)
# STRATEGY=qwen-vertex
GCP_QWEN_MODEL=qwen/qwen3-coder-480b-a35b-instruct-maas
#
# Option B: GPT OSS 120B (Alternative - General purpose)
# STRATEGY=gpt-oss-vertex
GCP_GPT_OSS_MODEL=openai/gpt-oss-120b-maas

# Linera Configuration
LINERA_RPC_URL=http://localhost:8080
LINERA_GRAPHQL_URL=http://localhost:8080/graphql

# API Server Configuration
# Port for REST API server (used by Linera service layer)
API_PORT=3001
API_HOST=0.0.0.0

# Redis (for caching and pub/sub)
REDIS_URL=redis://localhost:6379

# PostgreSQL (for historical data)
DATABASE_URL=postgresql://localhost:5432/alphabot

# Scheduler
PREDICTION_INTERVAL_SECONDS=3600

# Logging
LOG_LEVEL=info
